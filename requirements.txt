# Install CUDA PyTorch separately first; see README.md.
numpy>=1.22,<2
scipy>=1.5.2,<2
opencv-python>=4.5.2,<5
timm==1.0.11
tqdm>=4.66,<5
# Install the CUDA extensions separately after PyTorch; see README.md.
